/* multifloats — compatibility shim.
 *
 * The unified C/C++ public header now lives at
 * <multifloats/float64x2.h>. This file is kept so that existing code
 * that does `#include "multifloats.h"` or `#include <multifloats.h>`
 * continues to compile unchanged.
 */
#pragma once
#include "multifloats/float64x2.h"
