---
orphan: false
---

<!-- The canonical changelog lives at the repository root (CHANGELOG.md);
     this stub pulls it into the published site so the release history is
     visible from the user manual without duplicating the file. -->

```{include} ../CHANGELOG.md
```
